<%= form_with(model: production_company, local: true) do |form| %>
  <% if production_company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(production_company.errors.count, "error") %> prohibited this production_company from being saved:</h2>

      <ul>
      <% production_company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :production_company_name %>
  </div>

  <div class="field">
    <%= form.label :website %>
    <%= form.text_field :website, id: :production_company_website %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address, id: :production_company_address %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
